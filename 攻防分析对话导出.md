# 超越攻防需求与事件层不可取消方案

> 本文件覆盖之前的攻防分析导出，整理最后一部分讨论。以下属于设计建议，不代表已经实现或编译验证；不修改 Java 源码。末尾原回复截断的代码改为完整设计说明，并纠正其中的事件恢复问题。

## 一、总体目标与边界

超越需要的是分层攻防，而不是单纯增大攻击力或生命值。需要覆盖事件、伤害源、实体方法、生命数据、死亡与复活、实体生命周期和同步。

同一 JVM 中其它 Mod 同样可以修改字节码或直接写字段，因此不能承诺对任意 Mod“绝对无敌”或“绝对无法取消”。可实现的目标是：明确支持的攻击路线、对应拦截点、可验证的覆盖范围和失败回退。

## 二、攻击侧需要补充的内容

### 1. 统一攻击上下文

建议新增 TranscendAttackProfile / TranscendCombatContext，将穿透能力和终结策略集中管理。

- bypassArmor：护甲穿透。
- bypassEnchantment：附魔保护穿透。
- bypassResistance：抗性穿透。
- bypassAbsorption：吸收生命穿透，需要额外处理实际扣血位置。
- bypassShield：盾牌穿透。
- bypassInvulnerability：无敌状态和伤害冷却穿透，二者应分别核对。
- bypassCreative：创造模式保护穿透，默认不应无差别启用。
- preventEventCancel：阻止事件取消。
- preventDamageReduction：阻止事件层降伤。
- forceActuallyHurt：进入实际扣血路径。
- forceHealthField：针对已识别生命存储的适配。
- disableHealing / disableRevival：有限时、明确目标的禁疗与复活抑制。
- executionMode：正常伤害、软击杀、强制击杀或硬删除。

这些是能力建议，不是仅设置布尔值就能生效的原版 API；每项必须有对应注入点或兼容实现。

### 2. 攻击路径

1. 普通伤害与独立穿透伤害。
2. 实际扣血路径：核对 actuallyHurt 或当前版本等价方法。
3. 自定义生命与护盾适配：EntityDataAccessor、Capability、明确识别的 Float/Double 字段。
4. SoftKill：尽量保留死亡事件、掉落、战利品和击杀归属。
5. ForceKill：强制推进死亡状态，并验证是否被复活或回滚。
6. HardDelete：仅作为显式终结选项，处理实体生命周期清理。
7. 有次数和时限的复查，不采用无限递归或无限扫描。

不应盲目改写实体的所有数值字段，也不应把删除 TickList 条目视为完整删除：实体追踪、区块索引、乘骑关系与客户端同步可能仍然存在。

### 3. 功能与安全约束

- 自己、队伍、召唤物与主人关系过滤。
- 伤害源身份与攻击 ID，而不是根据“当前手持武器”判断所有伤害归属。
- 递归保护、攻击链深度限制和重试预算。
- 射线、投射物、范围、持续伤害和技能攻击可共用同一攻击管线。
- 默认保留掉落和正常清理；硬删除单独开关。
- 防御优先级规则：被保护的超越玩家是否可以被超越攻击伤害，必须显式配置。

## 三、防御侧需要补充的内容

| 防御层 | 需要覆盖的入口 |
| --- | --- |
| 常规伤害 | 护甲、附魔、抗性、吸收盾、盾牌、伤害冷却、特殊伤害源 |
| 事件层 | LivingAttackEvent、LivingHurtEvent、LivingDamageEvent、LivingDeathEvent |
| 方法层 | hurt / attackEntityFrom、实际扣血方法、setHealth、kill、die、死亡 tick |
| 生命数据层 | 标准生命同步字段、自定义生命池、护盾、Capability、生命上限属性 |
| 死亡与复活 | 图腾、死亡取消、阶段切换、恢复生命、替身或克隆 |
| 生命周期 | remove / discard、世界实体索引、Tick 列表、追踪和同步 |
| 状态一致性 | 服务端权威、跨维度、重登、保存加载、死亡界面和客户端生命显示 |

防御不能只依靠每 tick 回血：实体可能在下一 tick 之前已经被移除。必须保护关键写入与生命周期入口，同时允许正常退出、卸载和维度切换，避免永久残留实体。

## 四、为什么 setCanceled(false) 不够

普通监听器执行 event.setCanceled(false) 只能恢复当前状态。后续监听器仍然可以取消事件或把伤害改成 0。

HIGHEST / LOWEST 只是优先级排序，不保证绝对最先或最后；已经取消的事件还可能让未开启 receiveCanceled 的监听器跳过。

因此必须在事件发布前识别受保护攻击，并在事件对象的取消与伤害修改入口施加约束。

## 五、推荐结构：事件扩展接口 + 精确 Mixin

建议接口提供：

```java
public interface TranscendEventControl {
    boolean transcend$isUnCancelable();
    boolean transcend$isOnlyAmountUp();
    float transcend$getMinimumAmount();
    void transcend$lock(boolean onlyAmountUp, float minimumAmount);
}
```

设计要点：

- 标志属于单个事件实例，不使用全局开关。
- lock 在事件发布前设置，锁定后不向一般调用方暴露随意解锁入口。
- LivingAttackEvent / LivingDeathEvent 主要保护取消状态。
- LivingHurtEvent / LivingDamageEvent 还保护可变伤害数值。
- 根据伤害源中的可信上下文识别超越攻击；不要让所有事件都不可取消。
- 若用 ThreadLocal 传递上下文，使用栈并在 finally 中退出，防止反伤、连锁攻击或异常污染后续事件。

## 六、拦截取消操作

在 Event#setCanceled(boolean) 的入口拦截。下列为逻辑示意，Event 包名、Mixin 配置、描述符和 remap 设置必须按项目 Forge 版本调整。

```java
@Inject(method = "setCanceled(Z)V", at = @At("HEAD"),
        cancellable = true, remap = false)
private void transcend$guardCancel(boolean canceled, CallbackInfo ci) {
    Object self = this;
    if (canceled && self instanceof TranscendEventControl
            && ((TranscendEventControl) self).transcend$isUnCancelable()) {
        ci.cancel();
    }
}
```

关键：只阻止 setCanceled(true)，必须允许 setCanceled(false)。

前一版示例无条件阻止 setCanceled，会连恢复取消状态也一起阻止，因此不能直接照抄。

不要简单把 isCancelable() 改成 false：其它监听器继续调用 setCanceled(true) 时，某些 Forge 实现会抛异常。

如需加强，可以让受保护事件的 isCanceled() 返回 false，但这仍不是防反射、Unsafe 或其它字节码替换的安全边界。

## 七、拦截伤害降低

分别处理 LivingHurtEvent#setAmount 与 LivingDamageEvent#setAmount。

```java
// 伪代码：在 setAmount 的 HEAD 判断，满足条件时 ci.cancel()
if (control.isOnlyAmountUp()) {
    float floor = Math.max(event.getAmount(), control.getMinimumAmount());
    if (Float.isNaN(newAmount) || newAmount < floor) {
        rejectChange();
    }
}
```

- minimumAmount 在该阶段事件创建或发布前记录。
- 同时比较当前值，才能做到伤害增加后不能再降低。
- NaN 必须单独处理，因为 NaN 与数值的大小比较均为 false。
- 是否允许正无穷应单独规定，推荐采用有限伤害上限和独立终结模式，避免 Infinity 运算产生 NaN。
- 不要在 setAmount 注入里再次调用 setAmount，以免递归。
- Hurt 和 Damage 是不同阶段；事件间仍有护甲、吸收等逻辑，锁住 setter 不等于绕过这些减伤。
- 如果要求最终伤害最低值，必须单独在实际扣血处落实，不能把两个阶段锁混为一谈。

## 八、何时启用锁

推荐在以下位置之一启用：

1. 目标事件构造完成时，检查伤害源并初始化锁。
2. ForgeHooks 构造事件后、调用 EVENT_BUS.post 前启用。
3. 确认版本和加载时机后，在 EventBus.post 入口识别并初始化。

不要只依赖 HIGHEST 监听器：同优先级其它监听器可能先执行，事件也可能已经被取消。

## 九、分发结束后的兜底及其限制

优先选择 ForgeHooks 对具体伤害事件的发布与消费位置做兜底，避免修改整个 EventBus。

若必须注入 EventBus.post：

- 精确选择实际使用的重载与返回路径。
- 若 post 返回取消结果，必须同时纠正返回值；只在 TAIL 修改事件字段并不能保证已经计算出的返回值改变。
- 需要覆盖多个返回出口时使用适当的 RETURN 注入，而不是假定 TAIL 覆盖全部路径。
- 结束后恢复不能补回已经跳过的监听器，也不能撤销监听器已经造成的副作用。
- 分发后恢复伤害最低值只能作为兜底，不能替代 setter 入口保护。

## 十、事件不可取消不能解决的情况

以下路径可能完全不经过受保护事件：

- hurt / attackEntityFrom 在发布事件前直接返回 false。
- 自定义实体重写方法并拒绝执行父类逻辑。
- 直接 setHealth 或自定义生命字段写入。
- Capability 护盾吞掉伤害。
- 死亡方法、复活 tick 或阶段转换恢复目标。
- 直接移除实体或篡改实体管理结构。

因此事件锁只是攻击穿透的一层，必须和方法、生命数据、死亡与生命周期策略配合。

## 十一、断曜流光分析的重要更正

此前导出和回复存在几处过度推断，应以下列结论为准：

- setHealth(max(0.001F, health * 0.97F)) 是扣除当前生命的 3%，下限 0.001；不是把生命压到 0.1%。
- onAttack 的 switch 分支有贯穿执行：0 可以继续进入 1，1 可以继续进入 2；分支 2 可见的是施加 CURSED，不应描述成尚未确认的击飞分支。
- isOwned 在已读取的 onInventoryTick 中用于选取自有实体；不能据此断言它对全部攻击提供友军免伤。
- CURSED 的具体禁疗行为需要继续读取效果实现，不能仅根据名称认定禁疗。
- bypassAll 的实际范围取决于它修改的标签查询及其调用位置，并非自动绕过吸收盾或任意第三方防御。

## 十二、建议实施顺序

P0：统一保护身份、攻击上下文、友军规则、重入限制和日志。

P1：事件发布前锁定；拦截取消与降伤；补全死亡事件和实际扣血处验证。

P2：增加明确的生命系统适配、最大生命保护、禁疗与复活检测。

P3：分级终结和生命周期清理，验证掉落、实体追踪与客户端同步。

P4：在统一管线上扩展位移、范围、投射物与法术技能。

至少测试：事件预取消、同优先级取消、降伤为零、NaN、反伤递归、图腾、Boss 阶段、伤害方法提前返回、重登和跨维度。只有测试通过的组合才能声明支持，不以“绝对无敌”替代覆盖说明。

# 启示录、梦幻终焉、妖精归乡与艾洛洛斯之枪补充分析（2026-09-14）

## 目标范围校正

“终末之环”按用户指代应理解为启示录（Goety Revelation/RevelationFix）体系，而不是 `fantasy_ending` 的 `TwistedSoulRing`。当前本地已确认启示录内嵌包为 `META-INF/jarjar/[Forge]RevelationFix-1.20.1-4.4.jar`。

### 启示录中已定位的相关类

- `com.mega.revelationfix.common.item.other.CuriosStorageCrystal`
- `com.mega.revelationfix.common.compat.youkai.YoukaiKiller`
- `com.mega.revelationfix.common.entity.projectile.GungnirSpearEntity`
- `com.mega.revelationfix.common.item.tool.combat.trident.GungnirItem`
- `com.mega.revelationfix.common.apollyon.common.BypassInvulArrow`
- `com.mega.revelationfix.common.capability.entity.GRPlayerCapability`
- `com.mega.revelationfix.mixin.PlayerMixin`
- `com.mega.revelationfix.mixin.gr.PlayerMixin`
- `com.mega.revelationfix.mixin.advanced.invul.goety.ApostleMixin`
- `com.mega.revelationfix.mixin.advanced.invul.goety.EnderKeeperMixin`
- `com.mega.revelationfix.mixin.advanced.invul.goety.VizierMixin`
- `com.mega.revelationfix.safe.entity.IInvulnerableTickEntity`
- `com.mega.revelationfix.safe.entity.PlayerInterface`

## 一、炼狱聚晶/储存晶体的改血方向

本地类名明确定位为 `CuriosStorageCrystal`，但“炼狱聚晶”中文名称与实际注册名仍需通过启示录 lang、注册表或运行时物品 ID 精确确认。该类属于饰品/Curios 物品侧；其改血效果不能仅根据物品类名认定，必须结合其事件监听、Capability、Mixin 和对应注册代码判断。

目前可确认的相关生命修改支撑层包括：

- `GRPlayerCapability`：玩家扩展状态/能力存储。
- `PlayerInterface`：玩家 Mixin 暴露的扩展接口。
- `IInvulnerableTickEntity`：无敌 tick 或无敌状态的扩展标记。
- `PlayerMixin` 与 `gr.PlayerMixin`：玩家伤害、生命、同步和状态入口。
- `BypassInvulArrow`：专门为绕过无敌 tick 的伤害路径。

因此炼狱聚晶的改血应按“物品触发器 + 玩家/实体扩展状态 + 实际生命写入”三段理解，而不能只看 `setHealth`：

```text
装备/触发聚晶
  -> 写入 Curios 或 Capability 状态
  -> 修改生命/生命上限/无敌 tick 相关值
  -> 通过 PlayerMixin 或实体接口维护状态
  -> 同步并在后续 tick 校正
```

对 Transcend 的借鉴：改血功能应明确区分 `currentHealth`、`maxHealth`、`absorption`、`shield` 和自定义生命池；每种字段都要记录来源、原值、写入点和服务端权威性。没有反编译到具体方法前，不应声称聚晶一定绕过所有保护。

## 二、YoukaiEntity/YoukaiKiller 的清除逻辑

启示录实际定位到：

```text
com.mega.revelationfix.common.compat.youkai.YoukaiKiller
```

这是兼容层终结器，设计意图是将妖怪/Youkai 实体交给其 Mod 专用清除逻辑，而不是盲目调用原版 `kill()`。此前 `ValetteinItem.onLeftClickEntity` 在 Youkai 加载时调用 `YoukaiKiller.killYoukai(entity)`，说明攻击链存在“特殊实体 -> 专用终结器”的分派。

其可借鉴的生命周期模型：

```text
识别 Youkai 类型/接口
  -> 调用兼容 Mod 的死亡或清除入口
  -> 处理实体状态、归属、Boss/特殊阶段
  -> 必要时补充 remove/discard/purge
  -> 延迟复查是否重新生成或复活
```

不能把 `YoukaiKiller` 简化为通用 `discard`；兼容清除的价值正是避免跳过目标 Mod 的内部状态、死亡事件和客户端同步。Transcend 应建立 `EntityTerminationAdapter`，为 Youkai、Goety、Boss 和普通实体分别注册适配器。

## 三、艾洛洛斯之枪/冈格尼尔相关攻击与防御

启示录中已定位：

- `GungnirItem`
- `GungnirSpearEntity`
- `GungnirItem$1`
- `GungnirSpearRenderer`

同时存在防御/无敌绕过相关：

- `BypassInvulArrow`
- `IInvulnerableTickEntity`
- `advanced.invul.goety` 下的 Apostle、EnderKeeper、Vizier Mixin

这说明该武器体系不是单纯近战伤害，而是：

```text
投掷/实体化武器
  -> GungnirSpearEntity 持续管理飞行、命中和生命周期
  -> 伤害源或箭实体附加绕过无敌语义
  -> 对 Goety 特殊实体使用专门 Mixin 保护/穿透规则
  -> 命中后可能进入事件、实际扣血和特殊终结链
```

当前证据可以确认“实体投射物 + 无敌穿透适配”架构；具体伤害数值、改血算法、命中后是否删除目标，需要对 `GungnirItem`、`GungnirSpearEntity` 和 `BypassInvulArrow` 完整反编译后再下结论。

## 四、梦幻终焉中的防御

本地 `fantasy_ending-1.20.1-2.7.20e-all.jar` 已定位：

- `com.mega.uom.mixin.PlayerMixin`
- `com.mega.uom.util.entity.PlayerInvulnerableEntityData`
- `com.mega.uom.common.command.player.SetInvulnerablePlayerCommand`
- `com.mega.uom.common.items.curios.TwistedSoulRing`
- `com.mega.uom.common.items.magic.curios.FantasyDreamRing`

已确认的防御设计是：

```text
FE_INVULNERABLE_DATA：同步 Boolean
FE_INVULNERABLE：NBT 持久化键
PlayerMixin：玩家 hurt/攻击/同步/存档入口
PlayerInvulnerableEntityData：读写无敌状态
SetInvulnerablePlayerCommand：服务端命令开关
```

其中 `TwistedSoulRing` 本身更像 Curios 物品/状态载体；不能把它直接等同为完整 Mixin 防御。梦幻终焉真正值得借鉴的是“同步状态 + NBT 持久化 + Player.hurt 方法入口拦截”，但当前证据不能确认它覆盖死亡事件、tickDeath、实体删除、Capability 或所有第三方自定义生命。

## 五、妖精的归乡防御

当前根目录 JAR 与启示录内嵌包中没有能可靠对应中文“妖精的归乡”的唯一 Mod 文件/类名，不能凭名称编造实现。已确认启示录有 `FeSafeClass` 这一 `fantasy_ending` 兼容类，但它只说明跨 Mod 兼容入口存在，不能推出妖精归乡的完整防御逻辑。

后续精确分析需要该 Mod 的 JAR，或其注册名/类名。拿到后应重点检索：

```text
LivingAttackEvent / LivingHurtEvent / LivingDamageEvent / LivingDeathEvent
hurt / actuallyHurt / setHealth / kill / die / tickDeath
Capability / EntityDataAccessor / PlayerMixin / ServerPlayerMixin
invulnerable / shield / revive / remove / discard
```

## 六、统一结论：Transcend 还需要什么

要覆盖这些 Mod，应将 Transcend 分为以下适配层：

1. `DamageSourceAdapter`：护甲、抗性、无敌 tick、吸收和特殊 DamageType。
2. `EventLockAdapter`：事件不可取消、伤害最低值、死亡事件锁。
3. `HealthFieldAdapter`：原版生命、最大生命、护盾、Capability、Float/Double 自定义字段。
4. `PlayerDefenseAdapter`：同步 Boolean、NBT 持久化、Player.hurt 和死亡入口。
5. `EntityTerminationAdapter`：YoukaiKiller、Goety 专用终结、普通实体、Boss 分支。
6. `ProjectileAttackAdapter`：Gungnir 类实体投射物的命中与复查。
7. `LifecycleVerifier`：死亡、复活、克隆、TickList、追踪和客户端同步复查。

最终推荐流程：

```text
识别目标 Mod/实体类型
  -> 读取归属和保护状态
  -> 构造带能力标志的伤害源
  -> 事件层锁定
  -> 实际扣血/自定义生命适配
  -> 专用终结器（如 YoukaiKiller）
  -> 死亡/复活/实体索引复查
  -> 有限次数延迟重试
```

## 二步总结（阶段二）

### 已完成的确定结论

- “终末之环”应按启示录/RevelationFix 内容继续分析，而非误指梦幻终焉戒指。
- 启示录具备 `YoukaiKiller` 专用实体终结适配器。
- 启示录具备 `GungnirItem + GungnirSpearEntity` 的实体投射物攻击体系。
- 启示录具备 `BypassInvulArrow`、`IInvulnerableTickEntity` 和 Goety 特殊实体 Mixin，明确存在无敌 tick/特殊实体防御交互。
- 梦幻终焉具备同步无敌字段、NBT 持久化、Player.hurt 方法入口保护和命令开关。

### 尚未虚构、需进一步反编译确认的内容

- 炼狱聚晶的确切注册名、事件、改血公式和具体生命字段。
- Gungnir/艾洛洛斯之枪的确切伤害、改血和命中后处理。
- YoukaiKiller 内部究竟调用 kill、die、discard、专用接口还是多阶段清理。
- 妖精的归乡的实际防御类；本地缺少可唯一识别的 Mod JAR。

因此本阶段总结明确区分“已由类名/调用关系确认”和“待完整反编译确认”，避免把存在兼容类误报成已经验证的绝对防御或绝对击杀。

# 第三阶段：终末之环、Youkai’s Homecoming 与武器/防御整体提取

## 1. 名称与来源确认

启示录的“终末之环”实际注册键为：

```text
item.goety_revelation.halo_of_the_end
com.mega.revelationfix.common.item.curios.OdamaneHalo
```

“炼狱聚晶”实际注册键为：

```text
item.goety_revelation.revelation_focus
```

而不是 `CuriosStorageCrystal`；后者是另一个通用存储晶体类。当前文档以注册键为准。

本地没有 Youkai’s Homecoming 的旧版 1.20.1 JAR，但通过公开 GitHub 仓库 `Minecraft-LightLand/Youkai-Homecoming` 获取了对应源码用于确认 `YoukaiEntity` / `BossYoukaiEntity`；启示录字节码中的类型名也明确为：

```text
dev.xkmc.youkaishomecoming.content.entity.youkai.YoukaiEntity
```

## 2. 终末之环（OdamaneHalo）保护逻辑

终末之环的核心并非戒指类本身，而是玩家扩展上下文：

```text
OdamaneHalo
  -> HaloEvents
  -> OdamanePlayerExpandedContext
  -> GRPlayerCapability / PlayerInterface
  -> PlayerMixin 与伤害事件入口
```

`OdamanePlayerExpandedContext` 的关键字段/能力包括：

```text
invulnerableTime
reviveInvulnerableTime
nextReviveCooldowns
REVIVE_INVULNERABLE_FLAG
BLASPHEMOUS_FLAG
INVULNERABLE_TO_TAGS
INVULNERABLE_TO_DAMAGE
```

### 伤害处理

`HaloEvents.onLivingDamage(LivingDamageEvent)` 对拥有 Odamane 状态的玩家处理：

1. 若上下文 `isInvulnerable()` 为真，将伤害设置为 `0`。
2. 否则根据维度给伤害缩放：主世界约保留 25%，下界约保留 1%，其它维度约保留 50%。
3. 再经过 `OdamanePlayerExpandedContext.damageScale` 限制。
4. `damageLimit(player)` 为 `min(20, maxHealth * 0.25)`，防止单次受到超过生命上限四分之一且最大 20 的伤害。

这说明终末之环采用：

```text
伤害源分类免疫
+ 事件阶段归零
+ 维度伤害缩放
+ 单次伤害上限
+ 上下文状态机
```

### 复活逻辑

上下文 `tick()` 会检查玩家死亡状态：

```text
玩家死亡
+ 复活冷却 <= 0
  -> 生命恢复到最大生命约 75%
  -> 播放复活事件
  -> onRevive()
  -> 触发短时间时间停止/保护
  -> 设置后续复活冷却
```

复活无敌时间和复活冷却逐 tick 递减。上下文还通过 Capability flags 保存复活保护与其它状态。它不是单纯永久 `setInvulnerable(true)`，而是“伤害上限 + 分类免疫 + 自动复活 + 复活保护”的组合。

### 事件层局限

`HaloEvents` 使用 `LivingDamageEvent.setAmount(0)`，如果其它监听器在它之后再次增加伤害，单独这个监听器并不等于绝对不可伤害。因此 Transcend 若要超越，应把终末之环的伤害上限算法与事件锁、`Player.hurt` 方法层、复活状态机结合，而不是只复制 `setAmount(0)`。

## 3. Youkai’s Homecoming：YoukaiEntity 的保护与实体清除

### 普通 YoukaiEntity

源码中的 `YoukaiEntity` 使用独立战斗生命：

```text
combatProgress
```

关键覆盖方法：

```java
actuallyHurt
actuallyHurtImpl
hurtFinal
hurtFinalImpl
setHealth
setCombatProgress
getCombatProgress
getHealth
isAlive
isDeadOrDying
tickDeath
die
```

实际扣血链为：

```text
actuallyHurt
  -> spellCard.hurt
  -> ForgeHooks.onLivingHurt
  -> 护甲减伤
  -> 魔法减伤
  -> ForgeHooks.onLivingDamage
  -> hurtFinal
  -> 吸收盾处理
  -> combatProgress 减少
```

`setHealth` 与 `getHealth` 被重定向到 `combatProgress`。`tickDeath` 和 `die` 只有在 `combatProgress <= 0` 时才继续执行；否则实体可以被 `validateData()` 恢复为非死亡状态。这是典型的自定义生命池保护。

### BossYoukaiEntity

Boss 进一步防止非法改血：

- `hurt` 有 `hurtCall` 重入标志和伤害冷却。
- 非允许调用路径的伤害进入 `notifyIllegalDamage`，不直接造成生命损失。
- 普通伤害按 `maxHealth / damageLimit()` 限幅，默认单次不超过最大生命的 1/20。
- 非弹幕伤害还会按配置继续除以 `nonDanmakuReduction()`，默认约 5。
- `actuallyHurt`、`hurtFinal` 重新检查 `hurtCall` 和有限浮点数。
- `setHealth` 拒绝非有限值，并在初始化后拒绝外部降低生命。
- `setCombatProgress` 检查底层 vanilla 生命与战斗生命不一致，记录非法伤害并设置异常标志。
- `aiStep` 清除所有效果，阻止药水类控制。
- `canBeAffected` 返回 false。
- 特定目标、无敌标签、伤害冷却和自身攻击均有拒绝规则。

### 启示录的 YoukaiKiller

`YoukaiKiller.killYoukai(Entity)` 对 `YoukaiEntity` 做多层清除：

```text
服务端 ServerLevel
  -> 从 EntityTickList 移除
  -> setRemoved(DISCARDED)
  -> remove(DISCARDED)
  -> 设置异常坐标
  -> refreshDimensions
  -> 若仍加入世界，写入异常 Pos NBT
  -> tick 一次
  -> setAddedToWorldField(false)
```

它不是简单 `kill()`，而是针对 Youkai 自定义生命、死亡保护和实体追踪结构的兼容性清除。Transcend 应将它抽象成 `EntityTerminationAdapter`，并把“专用清除”放在通用 `discard` 之前或之后由适配器决定。

## 4. 艾洛洛斯之枪的确认与防御含义

启示录语言和 Tetra 配方确认名称为：

```text
item.tetra.scroll.sword/aeglos.name
 tetra/schematic/sword/aeglos.name
```

即“艾格洛斯之枪”（Aeglos），不是 `Gungnir`；另有独立的“朗基努斯之枪”。启示录包中确实存在：

```text
GungnirItem
GungnirSpearEntity
BypassInvulArrow
```

当前已确认其架构为“实体投射物 + 命中伤害 + 无敌 tick 穿透适配”。但 Aeglos 的具体 Tetra 改写效果没有在当前反编译范围内取得完整实现类，不能将 Gungnir 的字节码效果直接冒充 Aeglos 的算法。

建议后续以 Tetra 模块/改造注册、物品属性注入和实际攻击事件为入口继续确认。对 Transcend 的设计启示是：武器改写应放进 `DamageSourceAdapter` 与 `ProjectileAttackAdapter`，不能只依赖物品攻击力。

## 5. Youkai’s Homecoming 的 Mixin 点

公开源码中与防御/实体管理直接相关的可用 Mixin 点主要是：

```text
mixin.PlayerMixin
mixin.effect.EntityMixin
mixin.effect.LivingEntityMixin
mixin.api.PersistentEntitySectionManagerAccessor
mixin.api.ServerLevelAccessor
```

但 Youkai 的核心防御主要通过实体类自身重写实现，而不是一个“无敌 Mixin”：

```text
YoukaiEntity.actuallyHurt / setHealth / die / tickDeath
BossYoukaiEntity.hurt / actuallyHurt / setHealth / setCombatProgress / tick
```

启示录兼容层的关键 Mixin 点则包括：

```text
revelationfix.mixin.PlayerMixin
revelationfix.mixin.gr.PlayerMixin
revelationfix.mixin.advanced.invul.goety.ApostleMixin
revelationfix.mixin.advanced.invul.goety.EnderKeeperMixin
revelationfix.mixin.advanced.invul.goety.VizierMixin
revelationfix.mixin.gr.HellfireMixin
```

## 6. 整体逻辑提取：建议 Transcend 的层次

```text
攻击/防御身份识别
  -> DamageSource 标签与能力配置
  -> 事件前锁定（取消、降伤、死亡）
  -> hurt/actuallyHurt 方法层
  -> 护甲、抗性、吸收、护盾和无敌 tick
  -> 标准生命与自定义生命池适配
  -> Boss/Youkai/Goety 专用适配器
  -> SoftKill / ForceKill / HardDelete
  -> TickList、实体区段、追踪和同步校验
  -> 复活/克隆/阶段切换有限次复查
```

推荐 Mixin 点表：

| 层 | 推荐目标 | 目的 |
| --- | --- | --- |
| 事件对象 | `Event#setCanceled`、`LivingHurtEvent#setAmount`、`LivingDamageEvent#setAmount` | 对带超越上下文的事件阻止取消/降伤 |
| 伤害入口 | `LivingEntity#hurt`、版本等价 `attackEntityFrom` | 阻断或路由伤害 |
| 实际扣血 | `LivingEntity#actuallyHurt`、Youkai `hurtFinal` | 绕过或适配实际生命路线 |
| 生命写入 | `setHealth`、自定义 combatProgress/Capability 写入 | 防止非法改血与恢复回滚 |
| 死亡 | `die`、`tickDeath`、死亡事件 | 死亡锁与终结策略 |
| 实体管理 | `ServerLevel`/`EntityTickList`/实体区段管理器 | 专用清除和防复活残留 |
| 同步持久化 | `defineSynchedData`、读写 NBT、服务端玩家状态 | 无敌/生命状态重登和客户端一致 |
| 投射物 | Gungnir/Aeglos 对应实体与命中函数 | 统一远程攻击、无敌穿透和复查 |

### 最终阶段结论

- 终末之环的强项是上下文状态机、分类免疫、伤害缩放、单次伤害上限和自动复活。
- Youkai’s Homecoming 的强项是自定义生命池、严格的实际扣血入口、非法改血检测、伤害冷却和 Boss 伤害限幅。
- 启示录 `YoukaiKiller` 的强项是跨层实体清除：TickList、RemovalReason、异常位置、tick 和 added-to-world 状态一起处理。
- 梦幻终焉的强项是同步无敌状态、NBT 持久化和 Player 方法入口保护。
- Transcend 若要整体超越，必须把这些能力组合成“事件 + 方法 + 数据 + 生命周期 + 适配器”五层体系，并为每个具体 Mod 提供有限范围的兼容适配器。

任何“绝对无敌/绝对击杀”结论都应改为可验证的覆盖声明：明确覆盖哪些事件、哪些方法、哪些自定义生命字段和哪些实体生命周期入口；对尚未取得 Aeglos 或妖精归乡旧版二进制的部分，不应声称已经确认其具体实现。
